# v0.8.0 hosted staging deployment acceptance

Date: 2026-08-31 (Asia/Kolkata)

This record proves that the isolated Cloudflare staging Worker was deployed
from a reviewed, green candidate and that its anonymous Hosted Basic surface
behaves as intended while identity credentials are absent. It does not certify
WorkOS signup, Turnstile/WAF, authenticated device or CLI sync flows,
production cutover, the canonical GitHub repository, or the public prerelease.

## Candidate and deployment identity

- Runtime candidate commit: `db394098277b87d952d587509bc403b887844db2`
- Candidate origin: `arbazkhan971/handoffgraph` `main`
- Exact-head CI: run `33346047282`; all five jobs passed, including both OS
  race suites, the Platform and deployment bundles job, and the checksummed
  release snapshot
- Local Platform suite: 39 files and 1,611 tests passed
- Production and staging Worker dry bundles: passed
- Staging Worker: `handoffgraph-api-staging`
- Stable staging hostname:
  `https://handoffgraph-api-staging.arbaz-khan.workers.dev`
- Cloudflare Worker version: `4525db9d-540a-4af1-9803-83de5b0c7e25`
- Version created: `2026-08-31T01:11:22.148Z`
- Deployment created: `2026-08-31T01:11:24.142Z`
- Version tag: `git-db394098277b`
- Deployment message:
  `source db394098277b87d952d587509bc403b887844db2; CI 33346047282`
- Traffic: 100% on the version above

`GET /healthz` returned the immutable version and source tag in
`x-handoffgraph-worker-version` and `x-handoffgraph-worker-tag`, respectively,
without changing the stable `{"status":"ok"}` response body. This joins the
live Worker response to the exact Git commit and CI run rather than inferring a
deployment from a successful upload.

## Live Hosted Basic preflight

The sanitized preflight was recorded at `2026-08-31T01:12:30.415Z` and passed
all seven checks:

| Check | Live result |
|---|---|
| `GET /healthz` | 200, `cache-control: no-store`, exact version/tag headers, `{"status":"ok"}` |
| `GET /` | 303 to `/account`, `cache-control: no-store` |
| `GET /account` | 200, strict deny-by-default CSP, `Hosted identity unavailable`, `New accounts closed` |
| `GET /v1/me` | 401 `unauthorized` |
| `GET /v1/plans` | 200 with the public Hosted Basic catalog |
| `GET /v1/analytics/summary` | 404 on the Basic deployment fence |
| Anonymous `POST /v1/event-batches` | 401 `unauthorized` |

An independent `GET /v1/auth/start` probe returned 503
`hosted_auth_unavailable`, confirming that absent WorkOS credentials fail
closed rather than enabling a fallback identity path.

The rendered `/account` browser surface had the title
`Sign in · HandoffGraph`, visibly showed `New accounts closed` and
`Hosted identity unavailable`, and contained zero scripts, Turnstile markers,
forms, or sign-in links. This is anonymous-surface evidence only; it does not
prove a real identity or challenge flow.

## Live control-plane and D1 evidence

| Check | Live result |
|---|---|
| Staging Worker secret list | Empty (`[]`) |
| Staging D1 migration ledger | 22 applied migrations, IDs 1 through 22, with no pending migration |
| Staging D1 integrity | Remote `PRAGMA foreign_key_check` returned zero rows |
| D1 triggers | 137 total; `devices_reject_identity_mutation`, `devices_reject_revocation_reversal`, and `idempotency_keys_require_active_device` present |
| Hosted beta capacity | Global maximum 50; active count 0 |
| Scheduled trigger | Exactly `*/5 * * * *` |

The staging deployment retained only the intended Basic bindings: the isolated
staging D1 database, the isolated staging R2 bucket, the Worker version metadata
binding, fixed staging origins, and `HOSTED_SURFACE = "basic"`. The scheduled
maintenance trigger remains `*/5 * * * *`.

## Remaining owner-gated acceptance

Staging is deliberately not a completed hosted launch. WorkOS credentials are
not configured, and Turnstile Siteverify plus reviewed WAF/rate controls are
not certified. The authenticated two-tenant browser/device/CLI lifecycle and
deletion proof, canonical GitHub repository, immutable prerelease, production
migrations and cutover, and Vercel retirement all remain outside this
acceptance record.

A machine-readable sanitized record is committed as
[`v0.8.0-hosted-staging-acceptance.json`](v0.8.0-hosted-staging-acceptance.json).
